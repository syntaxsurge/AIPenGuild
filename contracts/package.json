{
  "name": "aipenguild-contracts",
  "version": "1.0.0",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:moonbase": "hardhat run scripts/deploy.ts --network moonbaseAlpha",
    "deploy:westend": "hardhat run scripts/deploy.ts --network westend",
    "deploy:moonbase:log": "hardhat run scripts/deploy.ts --network moonbaseAlpha > deploy.log",
    "deploy:westend:log": "hardhat run scripts/deploy.ts --network westend > deploy.log",
    "update:addresses": "npx ts-node update-addresses.ts",
    "test": "hardhat test",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "@openzeppelin/contracts": "^4.9.2",
    "@types/node": "^20.0.0",
    "hardhat": "^2.12.7",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0"
  }
}
